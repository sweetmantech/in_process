import Loading from "../Loading";

const LoadingPage = () => {
  return (
    <div className="w-screen h-screen flex items-center justify-center">
      <Loading className="aspect-[1/1] w-16" />
    </div>
  );
};

export default LoadingPage;
